spring:
  application:
    name: orders
  datasource:
    url: ${ORDERS_DB_URL}
    username: ${ORDERS_DB_USER}
    password: ${ORDERS_DB_PASS}
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: true
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS}
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
